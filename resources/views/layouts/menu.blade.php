<div class="btn-group">
    <a href="{{ route('barang.index') }}" class="btn btn-primary">Barang</a>
    <a href="{{ route('status.index') }}" class="btn btn-primary">Status</a>
    <a href="{{ route('tracking.index') }}" class="btn btn-primary">Tracking</a>
</div>